type listMessage = [
    {
        key: string;
        name: string;
        button: boolean;
        quantity: string;
        description: boolean;
        category: boolean;
    }
];

export default listMessage;
